interface:
  display_name: "Opencommit"
  short_description: "Repo-specific patterns and workflows for opencommit"
  default_prompt: "Use the opencommit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true